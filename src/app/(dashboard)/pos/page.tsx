import POSClient from '../../../components/custom/POSClient';

export const metadata = {
  title: 'POS (Bán hàng) | Hệ thống quản lý',
  description: 'Giao diện tính tiền và quản lý giỏ hàng',
};

export default function POSPage() {
  return <POSClient />;
}
